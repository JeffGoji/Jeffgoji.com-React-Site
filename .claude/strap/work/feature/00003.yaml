id: "00003"
type: feature
title: "Establish the dark motorsport-editorial design system and rebuild the app shell (nav / header / footer)"
state: active
parent: null
iteration: null
area: null
assigned_to: null
tags:
  - AI
  - strap:feature
links:
  - target: "00002"
    type: related
  - target: "00007"
    type: related
  - target: "00008"
    type: related
  - target: "00009"
    type: related
  - target: "00010"
    type: related
  - target: "00011"
    type: related
created_at: 2026-08-01T18:45:00Z
created_by: spec-lead
updated_at: 2026-08-01T19:12:00Z
updated_by: dev-lead
resolved_at: null
closed_at: null
description: |
  | Field        | Value                       |
  |--------------|-----------------------------|
  | Authored By  | spec-lead                   |
  | Authored At  | 2026-08-01T18:45:00Z        |
  | Completed By | _(set at resolution)_       |
  | Completed At | _(set at resolution)_       |

  ---

  # Establish the dark motorsport-editorial design system and rebuild the app shell (nav / header / footer)

  ## Description

  Feature A — the site-wide chrome and visual foundation every surface renders inside. Ships the black + red premium design-token layer (charcoal `#141418` canvas, reading panel `#202027`, brand red `#E10600`, Archivo/Inter/Space Mono self-hosted), the new "The Goji Line" SVG logo (replacing `logo.gif`), and the rebuilt shell: sticky blur `NavMenu` with red-underline active state + the permanent red "What's New" nav pill, redesigned `Header`, and the currently-dormant `Footer` wired in for the first time. Removes the "NA Miata (coming soon)" placeholder and the orphaned `/totdtrip` route.

  ## Linked Spec

  **Spec:** 00002 (`related`)

  ## Constituent Parts assigned

  | Part | Domain | Owner agent type | Summary |
  |---|---|---|---|
  | P1 — Design System & Visual Language | client-ui | designer | tokens, premium-red palette, SVG logo, type scale, responsive tokens (mockups approved) |
  | P5 — Styling-system reconciliation | client-ui | frontend-engineer | collapse Bootstrap-utils + `scss/styles.scss` + legacy `assets/css/style.css` + inline into the token layer; **fix `styles.scss:2` — declare Sass `$theme-colors` BEFORE `@import bootstrap`**; copy `tokens.css` `:root` vars (incl. `.media--editorial` grade vars) into SCSS |
  | P2 — Shell / Navigation / Footer redesign | client-ui | frontend-engineer | rebuild `NavMenu` (drop `bg="dark"`, add What's New pill, remove coming-soon), `Header`, wire in `components/Footer/index.jsx`, swap `logo.gif`→`assets/logo.svg` |

  ## Acceptance Criteria subset

  - **AC-002:** The redesigned shell (`NavMenu`, `Header`, a rendered `Footer`) applies the new logo (no `logo.gif`) and the premium black+red palette; the "NA Miata (coming soon)" placeholder is removed.
  - **AC-018:** The orphaned `/totdtrip` route (`App.jsx:61`) and the "coming soon" placeholder are removed.
  - _Contributes to (verified in sibling Features):_ AC-001 (foundation for B's route audit), AC-005 (responsive shell + navbar→mobile-menu collapse, verified in B), AC-014 (the permanent What's New nav pill; route existence owned by D).

  ## Mockup References

  - `.claude/strap/mockups/spec-00002/design-system.html` — palette/type/components/states/logo reference.
  - `.claude/strap/mockups/spec-00002/assets/tokens.css` — DESIGN TOKENS SOURCE OF TRUTH (map 1:1 to Sass vars + `$theme-colors`; header documents the import-order gotcha).
  - `.claude/strap/mockups/spec-00002/assets/app.css` — component styles (nav/header/footer chrome).
  - `.claude/strap/mockups/spec-00002/assets/logo.svg` — "The Goji Line" logo (recolours via token).
  - `.claude/strap/mockups/spec-00002/assets/app.js` — `renderShell()` behavior → rebuilt NavMenu + Header + Footer.
  - `.claude/strap/mockups/spec-00002/index.html` — hub reference for nav inventory.

  ## Out of scope

  Per-route/page body content (B). Image pipeline / srcset / OG (C). Analytics (C). The `/whats-new` route + page content (D) — A only adds the nav pill pointing at it. Gallery-hub routing/redirects (B).

  ## Dependencies

  Depends on Feature C's **baseline slice** (V1 perf/engagement baseline must be captured BEFORE this V2 UI lands — Assembly Phase 0 → Phase 1). Blocks Feature B (per-surface needs P1+P5+P2). P8 security-reviewer + P9 test-strategist parallelize (route-parity guard asserting `/totdtrip` removed lands at the A/B seam).

  ## Quality Checklist

  - [x] Description concrete enough for a senior developer to scope.
  - [x] Linked Spec 00002 is resolved.
  - [x] Constituent Parts reference real Spec rows (P1/P5/P2).
  - [x] Acceptance Criteria use the Spec's stable IDs verbatim.
  - [x] Out-of-scope populated.
  - [x] Dependencies explicit.
  - [x] Mockup References enumerate real files.
comments:
  - id: c1
    author: dev-lead
    timestamp: 2026-08-01T18:45:00Z
    body: "[STRAP/agent:spec-lead] Feature authored via /generate-features from Spec 00002. State: new. Owns AC-002, AC-018 (P1/P5/P2). Owners: designer + frontend-engineer (both active)."
  - id: c2
    author: dev-lead
    timestamp: 2026-08-01T19:12:00Z
    body: "[STRAP/agent:dev-lead] State: new -> active (via /decompose-feature). Decomposed into 5 Stories (00007-00011), 14 Tasks (00012-00025), ~55 senior-dev-hours. Dep chain: 00007+00008 -> 00009 -> 00010 -> 00011. Reconciled specialist overlap (designer authors partials; frontend assembles)."
  - id: c3
    author: dev-lead
    timestamp: 2026-08-03T16:00:00Z
    body: "[STRAP/agent:dev-lead] Sprint allocation (via /plan-sprint): Story 00007 (3 Tasks, 10h, whole) + Task 00015 (4h, split from Story 00008) moved to Sprint 2026.08.A -- 14h of 16h remaining sprint capacity. Developer: unassigned (JeffGoji + agent execution team). Remaining backlog under this Feature: Task 00016 (3h, rest of Story 00008), Stories 00009/00010/00011 (37h, blocked in sequence) -- overflow, held for next /plan-sprint. Note: allocating 00007 does not unblock 00009, which needs both 00007 and 00008 complete; 00008 stays split across sprints (00015 in, 00016 out) rather than deferred whole, per CPO approval of the sprint-planner proposal."
  - id: c4
    author: dev-lead
    timestamp: 2026-08-03T20:00:00Z
    body: "[STRAP/agent:dev-lead] PARTIAL execution complete (via /execute-sprint, Sprint 2026.08.A). Story 00007 (all 3 sprint-allocated Tasks: 00012 _tokens.scss, 00013 _theme.scss, 00014 _grade.scss) resolved. Task 00015 (self-hosted fonts, Story 00008) resolved; Story 00008 stays active -- sibling Task 00016 (logo) remains unallocated overflow. All 4 Tasks merged into feature/00003-design-foundation-shell (3542094), 2 waves (00012 solo, then 00013/00014/00015 in parallel). Centralized build+test pass re-verified independently by dev-lead after every merge and again at integration audit: 245/245 tests green, production `vite build` clean. PR #17 opened against feature/strap-onboarding (stacked -- main still lacks the STRAP install). No AC fully closed this round by design -- this slice is the P1 token/theme/grade/font foundation, contributing to AC-001 (foundation) and AC-002 (palette/token half); neither closes until Story 00009 (styling reconciliation + styles.scss wiring) and Story 00010 (NavMenu/Header render) land. Feature state stays ACTIVE, not resolved -- Stories 00009/00010/00011 (37h) remain unallocated overflow; a future /plan-sprint + /execute-sprint pass continues this Feature once sprint capacity allows. CreateTeam was not attempted this run (documented harness unreliability from Feature C); manual git worktrees + background Agent dispatches used instead, matching the established workaround. designer specialist exceeded its 200K per-agent budget across the run's 4 dispatches (266K total) -- noted in usage.yaml; no further designer work was needed in this run's scope so no redispatch was blocked."
  - id: c5
    author: dev-lead
    timestamp: 2026-08-04T04:00:00Z
    body: "[STRAP/agent:dev-lead] CPO-DIRECTED CAPACITY OVERRIDE (bypasses /plan-sprint's single-sprint capacity gate). CPO needs the V2 shell visibly live urgently; normal flow would wait for Sprint 2026.08.A's 2026-08-08 boundary. Allocated Task 00016 (3h) + Story 00009/Tasks 00017-00018 (10h) + Story 00010/Tasks 00019-00022 (15h) = 28h to Sprint 2026.08.A despite only 2h nominal capacity remaining (sprint now shows 68h against a 42h nominal total -- 26h over). Justification: original_estimate is a human senior-dev-hours planning fiction for velocity tracking, not a real wall-clock constraint on agent execution speed (agents complete tasks in minutes regardless of estimate) -- see dev-lead memory v2-initiative.md critical-context notes. Proceeding straight to /execute-sprint for this scope: chain is 00016 -> 00017 -> 00018 -> 00019 -> 00020 -> {00021, 00022}, designer then frontend-engineer. This closes out Story 00008 and, on completion, Stories 00009+00010 -- the wiring + shell render that actually makes the V2 design visible in the built site."
  - id: c6
    author: dev-lead
    timestamp: 2026-08-04T08:05:00Z
    body: "[STRAP/agent:dev-lead] CAPACITY-OVERRIDE EXECUTION COMPLETE. All 7 Tasks resolved (00016, 00017, 00018, 00019, 00020, 00021, 00022); Stories 00008, 00009, 00010 all fully resolved. THE V2 DESIGN IS NOW VISIBLY LIVE in the compiled build on feature/00003-shell-wiring-and-nav -- verified directly (not assumed) at every merge: --bs-primary: #E10600 in the compiled CSS, single CSS/JS entry path, logo/fonts/nav/header all rendering under the token system, position:sticky nav confirmed not silently broken. 357/357 tests green, production vite build clean throughout. One real regression caught and fixed mid-execution (Task 00022): a naive Header wrapper would have silently broken the nav's position:sticky by becoming its containing block with a zero-length offset range -- fixed via display:contents. One transient infra failure (Task 00017's first dispatch, API ENOTFOUND) recovered via clean redispatch with zero work lost. frontend-engineer ended this round at 430K against its 200K per-agent budget across 6 dispatches (00017 retry 72K, 00018 78K, 00019 62K, 00020 78K, 00021 62K, 00022 78K); designer added 62K (00016). Session total 492K. Story 00011 (Footer + /totdtrip removal + route-parity guard, 12h) remains the only unallocated overflow left in this Feature. Feature state stays ACTIVE, not resolved, pending Story 00011 in a future pass."
