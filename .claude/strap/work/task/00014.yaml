id: "00014"
type: task
title: "Port the .media--editorial grade layer into _grade.scss"
state: resolved
parent: "00007"
iteration: "Sprint 2026.08.A"
area: null
assigned_to: null
tags: [AI, strap:task]
links:
  - target: "00012"
    type: predecessor
original_estimate: 3
completed_work: 3
created_at: 2026-08-01T19:10:00Z
created_by: dev-lead
updated_at: 2026-08-03T19:40:00Z
updated_by: dev-lead
resolved_at: 2026-08-03T19:40:00Z
closed_at: null
description: |
  | Field | Value |
  |---|---|
  | Authored By | dev-lead |
  | Authored At | 2026-08-01T19:10:00Z |
  | Completed By | designer |
  | Completed At | 2026-08-03T19:40:00Z |

  ---

  # Port the .media--editorial grade layer into _grade.scss

  ## Description
  Create `src/scss/_grade.scss` porting the reusable editorial image-grade system from `app.css:201-242`: the `--grade-*` `:root` vars, base `.media--editorial` (`position:relative; isolation:isolate`), `.media--editorial > img` filter (brightness .95/contrast 1.06/saturate .90), the `.media--editorial::after` multiply-blend tone+vignette overlay, the `--hero`/`--soft` modifiers, and the reduced-motion guard. This is the class P3/ST-4 apply to `.hero__media`/`.card__media`/`.thumb`/`.post__media`.

  ## Linked Story
  **Story:** 00007

  ## Original Estimate
  **Hours (senior-developer):** 3

  ## Implementation guidance
  Verbatim port. CRITICAL: overlay MUST be `::after` (not `::before`) so it survives child stacking (number-tags/index-chips); keep `isolation:isolate` so the multiply blend can't leak past the media box.

  ## Dependencies
  Predecessor: 00012 (grade overlay references `--dur`/`--ease-out` from tokens).

  ## Definition of Done
  `_grade.scss` compiles; a probe `<figure class="media--editorial"><img></figure>` renders the filter + a multiply `::after` overlay; `--hero`/`--soft` override the grade vars; `prefers-reduced-motion` removes the filter transition.

  ## Quality Checklist
  - [x] One-paragraph description; Story linked; estimate >=3h; real files; DoD testable.
comments:
  - id: c1
    author: dev-lead
    timestamp: 2026-08-03T19:35:00Z
    body: "[STRAP/agent:dev-lead] State: new -> active (via /execute-sprint). Wave 2. Dispatched to designer in worktree designer-00014 on branch task/00014-grade-scss."
  - id: c2
    author: dev-lead
    timestamp: 2026-08-03T19:40:00Z
    body: "[STRAP/agent:dev-lead] State: active -> resolved (via /execute-sprint). Merged from task/00014-grade-scss (b5b8558) into feature/00003-design-foundation-shell (e3cd2b3). Completed By: designer. 2 new files (_grade.scss, _grade.test.js), no scope creep. Verbatim port verified by 23-test parity+probe suite incl. semantic hero/soft-modifier directionality checks; ::after (not ::before) and isolation:isolate both confirmed. Deliberately does not @use tokens (--dur/--ease-out resolve as CSS custom properties at cascade time) -- styles.scss must load _tokens.scss for the reduced-motion transition to resolve; noted for Story 00009's wiring. Neither _tokens.scss nor _grade.scss is reachable from a compiled entry point yet -- Story 00009 (styles.scss assembly + import-order fix) is the seam that activates this Story's output. 109/109 repo tests green after merge."
