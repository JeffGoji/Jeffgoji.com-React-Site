/**
 * The numbered changelog ledger — placeholder.
 *
 * Task 00068 ports the `.cl-head` heading and the `.cl-item` rows from
 * whats-new.html and takes the `whatsNew[]` array (Task 00065) as a prop. What
 * stands here is the empty ledger that case leaves behind: the mockup's own
 * rail container, with no row on it yet.
 */
function Changelog() {
    return <div className="changelog" />;
}

export default Changelog;
